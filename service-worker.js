const CACHE = "project13-foundation-v1";
const ASSETS = ["./", "index.html", "css/style.css", "js/app.js", "manifest.webmanifest", "assets/background/room-window.png", "assets/sprites/cat.png", "assets/sprites/bottle.png", "assets/ui/world-record.png", "assets/ui/journey.png", "assets/ui/kitchen.png", "assets/ui/gallery.png", "assets/ui/boss.png", "assets/sound/bgm.mp3"];
self.addEventListener("install", (event) => event.waitUntil(caches.open(CACHE).then((cache) => cache.addAll(ASSETS)).then(() => self.skipWaiting())));
self.addEventListener("activate", (event) => event.waitUntil(caches.keys().then((keys) => Promise.all(keys.filter((key) => key !== CACHE).map((key) => caches.delete(key)))).then(() => self.clients.claim())));
self.addEventListener("fetch", (event) => { if (event.request.method !== "GET") return; event.respondWith(caches.match(event.request).then((cached) => cached || fetch(event.request).then((response) => { const copy = response.clone(); caches.open(CACHE).then((cache) => cache.put(event.request, copy)); return response; }).catch(() => caches.match("index.html")))); });
